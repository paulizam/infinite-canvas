{
  "name": "infinite-canvas-fusion",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "format:check": "prettier --check apps/api apps/worker packages/contracts packages/canvas-core packages/model-gateway",
    "quality": "pnpm format:check && pnpm typecheck && pnpm test && pnpm build",
    "security:secrets": "node ops/secret-scan.mjs",
    "licenses:check": "node ops/license-inventory.mjs --check",
    "requirements:check": "node ops/requirements-audit.mjs --check",
    "compose:check": "node ops/compose-contract.mjs",
    "release:check": "node ops/migration-manifest.mjs --check && node ops/license-inventory.mjs --check && node ops/requirements-audit.mjs --check && node ops/compose-contract.mjs && node ops/secret-scan.mjs && node ops/release-check.mjs"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "overrides": {
      "postcss": ">=8.5.18",
      "prismjs": ">=1.30.0",
      "sharp": ">=0.35.0"
    }
  }
}
