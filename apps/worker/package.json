{
  "name": "@infinite-canvas/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@infinite-canvas/contracts": "workspace:*",
    "@infinite-canvas/model-gateway": "workspace:*",
    "@infinite-canvas/workflow-runtime": "workspace:*",
    "jsjianyingdraft": "1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "tsx": "^4.21.0"
  }
}
