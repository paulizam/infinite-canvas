{
  "allowedUnknown": {
    "@anthropic-ai/claude-agent-sdk": "Upstream package metadata does not declare SPDX; usage is governed by Anthropic's published commercial terms.",
    "@anthropic-ai/claude-agent-sdk-darwin-arm64": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-darwin-x64": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-linux-arm64": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-linux-arm64-musl": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-linux-x64": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-linux-x64-musl": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-win32-arm64": "Platform package shipped with the explicitly approved Anthropic Agent SDK.",
    "@anthropic-ai/claude-agent-sdk-win32-x64": "Platform package shipped with the explicitly approved Anthropic Agent SDK."
  },
  "deniedLicensePatterns": ["AGPL", "SSPL", "BUSL", "Commons-Clause"]
}
